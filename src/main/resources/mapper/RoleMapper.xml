<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.zut.mfs.dao.RoleDao">
    <select id="listPermission" resultMap="cn.edu.zut.mfs.domain.Permission">
        select permission.*
        from  role_permission_relation left join permission on role_permission_relation.permission_id = permission.id
        where role_permission_relation.role_id = #{roleId}
    </select>
</mapper>